apiVersion: v1
kind: Service
metadata:
  name: homarr-service
  namespace: dashboard
spec:
  selector:
    app: homarr
  ports:
    - protocol: TCP
      port: 7575
      targetPort: 7575
      nodePort: 30100  
  type: NodePort
