apiVersion: v1
kind: Service
metadata:
  name: nextcloud-app
  namespace: storage
spec:
  type: NodePort
  selector:
    app: nextcloud-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30086 
